# IDE / editor
.idea
.claude

# Build artifacts
zesh
zesh_debug
src/*.o
*.o
cmake-build-debug

# Scratch / local-only files (not part of the repo)
test_final.sh
test_parite_v2.sh
improvements.txt

# Fuzzing build artifacts and run output (sources/corpus ARE tracked)
fuzz/fuzz_lexer
fuzz/fuzz_parser
fuzz/fuzz_expand
fuzz/*_asan
fuzz/findings/

# NOTE: test_parite.sh is intentionally tracked — the CI runs it.
# NOTE: *.sh is NOT globally ignored so the fuzz corpus (*.sh seeds),
#       run_fuzz.sh and triage_crashes.sh are tracked.
